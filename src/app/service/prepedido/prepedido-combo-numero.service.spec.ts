/* tslint:disable:no-unused-variable */

import { TestBed, async, inject } from '@angular/core/testing';
import { PrepedidoComboNumeroService } from './prepedido-combo-numero.service';

describe('Service: PrepedidoComboNumero', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [PrepedidoComboNumeroService]
    });
  });

  it('should ...', inject([PrepedidoComboNumeroService], (service: PrepedidoComboNumeroService) => {
    expect(service).toBeTruthy();
  }));
});
